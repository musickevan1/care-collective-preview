# Prompt: Homepage Background Implementation

> Generated by @prompt-builder on 2024-12-21

## Original Idea
Implement the homepage background redesign based on UI Architect's section architecture decisions.

## Refined Understanding
This prompt executes the implementation phase after UI Architect has designed the section layout. The Frontend Engineer will create React components, modify existing sections, and implement the background/divider system.

## Target Agent
`@frontend-engineer` - React/Next.js component implementation

---

## Prompt

### Task: Implement Homepage Section Backgrounds & Dividers

Implement the homepage background system based on the UI Architect's design in `01-ui-architect.md`. Create reusable components and update existing sections.

### Implementation Scope

#### 1. Create Section Background Components

**New Component: `components/ui/SectionDivider.tsx`**

```typescript
interface SectionDividerProps {
  variant: 'wave' | 'curve' | 'angle' | 'organic'
  position: 'top' | 'bottom'
  fromColor: string  // Tailwind color class
  toColor: string    // Tailwind color class
  height?: 'sm' | 'md' | 'lg'  // 12px, 20px, 32px
  flip?: boolean
  className?: string
}
```

Requirements:
- SVG-based dividers for crisp rendering
- CSS custom properties for colors
- Responsive height adjustments
- `prefers-reduced-motion` support
- Accessible (decorative, `aria-hidden="true"`)

**New Component: `components/ui/SectionBackground.tsx`**

```typescript
interface SectionBackgroundProps {
  variant: 'solid' | 'gradient' | 'pattern' | 'layered'
  color: string
  patternType?: 'dots' | 'grid' | 'organic'
  patternOpacity?: number
  children: ReactNode
  className?: string
  id?: string
}
```

Requirements:
- Consistent section wrapper
- Built-in pattern overlays (optional)
- Gradient direction control
- Z-index management for layered content

#### 2. Update Existing Sections

Modify `app/page.tsx` sections:

**Hero Section** (lines 151-153)
- Keep gradient background
- Add subtle organic pattern overlay
- Ensure smooth transition to next section

**What is CARE Section** (lines 154-260)
- Replace inline `borderRadius` dividers with `SectionDivider` component
- Maintain dusty-rose/20 background for cards area
- Improve card container styling

**About Section** (lines 262-354)
- Update sage-dark background implementation
- Add wave divider at top
- Ensure photo and text readable on dark background

**What's Happening Section** (lines 356-390)
- Implement sage/15 background with proper dividers
- Update to use new component system

**Resources Section** (lines 392-462)
- Add subtle background treatment (currently flat cream)
- Consider light pattern or gradient

**Contact Section** (lines 464-508)
- Navy background with improved divider
- Ensure CTA contrast on dark background

#### 3. Update LandingSection Component

Enhance `components/LandingSection.tsx`:
- Add divider props (top/bottom)
- Support new background variants
- Integrate with SectionBackground

```typescript
interface LandingSectionProps {
  // Existing props...
  dividerTop?: SectionDividerProps
  dividerBottom?: SectionDividerProps
  backgroundVariant?: 'solid' | 'gradient' | 'pattern'
  backgroundPattern?: 'dots' | 'grid' | 'organic'
}
```

#### 4. CSS Updates

Add to `app/globals.css`:

```css
/* Section divider utilities */
.section-divider-wave { /* SVG wave pattern */ }
.section-divider-curve { /* CSS curve */ }
.section-divider-organic { /* Organic blob shape */ }

/* Background patterns */
.bg-pattern-dots { /* Dot pattern overlay */ }
.bg-pattern-organic { /* Organic shapes */ }

/* Section transitions */
.section-transition-smooth {
  /* For scroll-based transitions if needed */
}
```

### File Changes Required

| File | Action | Description |
|------|--------|-------------|
| `components/ui/SectionDivider.tsx` | Create | New divider component |
| `components/ui/SectionBackground.tsx` | Create | New background wrapper |
| `components/LandingSection.tsx` | Modify | Add divider/background props |
| `app/page.tsx` | Modify | Update all 6 sections |
| `app/globals.css` | Modify | Add utility classes |

### Code Patterns to Follow

**React patterns (from codebase):**
```typescript
// Use ReactElement return type
import { ReactElement } from 'react'

function Component(): ReactElement {
  return <div>...</div>
}

// Use named exports from components/ui
export { SectionDivider } from './SectionDivider'
```

**Tailwind patterns (from codebase):**
```typescript
// Use cn() for conditional classes
import { cn } from '@/lib/utils'

className={cn(
  'base-classes',
  variant === 'dark' && 'dark-variant-classes',
  className
)}
```

**Color usage:**
```typescript
// Use CSS custom properties, not hardcoded hex
className="bg-sage-dark"        // Good
style={{ background: '#4A6B66' }} // Avoid
```

### Success Criteria

- [ ] All 6 sections render with new background system
- [ ] Dividers transition smoothly between colors
- [ ] No visible seams or gaps between sections
- [ ] Mobile rendering is clean (no overflow issues)
- [ ] Components are reusable for other pages
- [ ] TypeScript compiles with no errors
- [ ] Lighthouse performance score maintained

### Testing Checklist

After implementation:
```bash
# Type check
npm run type-check

# Dev server visual testing
npm run dev
# Check: localhost:3000

# Mobile responsive check
# Use Chrome DevTools device emulation
```

Visual tests:
- [ ] Hero → What is CARE transition
- [ ] What is CARE → About transition
- [ ] About → What's Happening transition
- [ ] What's Happening → Resources transition
- [ ] Resources → Contact transition
- [ ] Contact → Footer transition
- [ ] All transitions on mobile (375px, 768px)

---

## Context Notes

**Dependencies:**
- This prompt depends on `01-ui-architect.md` output
- Coordinate with `03-style-master.md` for polish

**Performance considerations:**
- SVG dividers should be inline (not external files)
- Avoid CSS filters on large areas
- Test with throttled CPU in DevTools

**Accessibility:**
- Dividers are decorative (`aria-hidden="true"`)
- Maintain text contrast on all backgrounds
- Test with Windows High Contrast mode
